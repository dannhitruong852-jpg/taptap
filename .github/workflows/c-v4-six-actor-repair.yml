name: C v4 six-actor repair round 1 (frozen)

on:
  pull_request:
    branches: ['c-v4-golden-set-qa2']
    paths:
      - '.github/workflows/c-v4-six-actor-repair.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: c-v4-six-actor-repair-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frozen:
    runs-on: ubuntu-latest
    steps:
      - name: Preserve completed round-one evidence
        run: |
          echo 'Round one is frozen at run 34744873122.'
          echo 'Use the round-two workflow for remaining actors 03, 07, 14, and 15.'
